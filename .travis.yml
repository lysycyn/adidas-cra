language: node_js
node_js:
  - "node"
before_install:
  - npm install -g now
install:
  - npm install
script:
  - npm run lint
before_deploy:
  - npm run deploy
# deploy:
#   - provider: script
#   - script: "npm run deploy"
#   - on:
#       branch: feat/4-deploy
